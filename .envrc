# Loaded by direnv when you cd into this repo. It reads the gitignored .env file
# and exports its values into the shell, where the CLI picks them up via
# process.env. Run `direnv allow` once to approve this file.
#
# See README "Inference backends" for the full list of variables.
dotenv_if_exists .env
